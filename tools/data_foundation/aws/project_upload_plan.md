# Future Project Upload Plan

Template only.

1. Confirm source data licenses and manifest checksums.
2. Package code and configuration without raw data unless explicitly approved.
3. Upload to a scoped S3 prefix such as `s3://REPLACE_WITH_BUCKET/arabic-ocr-distortion/project/`.
4. Upload checkpoints separately under `checkpoints/`.
5. Verify checksums after upload.

No upload is performed by this file.

