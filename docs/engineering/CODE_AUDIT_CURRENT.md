# Code Audit — Current Session (2026-09-21)

Static + dynamic audit of production code. Findings from four review passes
(filesystem/persistence, concurrency/state, input-handling/security,
data-pipeline) consolidated here. Evidence is file:line as of HEAD
`1899415` plus this session's fixes. Severity: P0 critical → P3 minor.

## Fixed this session (with regression tests)

| ID | Sev | Finding | Location | Fix + Test |
|---|---|---|---|---|
| F1 | P1 | User cancel did not stop the RQ job: worker continued / queued job later ran to completion against the user's cancel; RQ retries resurrected the job via `start_job`'s `cancelled → pending` flip | `pdfword/worker_api.py` (`cancel_user_document`, `start_job`) | Cancel now best-effort calls `get_distributed_queue().cancel(job_id)` (logged as `distributed_cancel_deferred` on failure); `start_job` refuses to resume a cancelled job (409) — only the explicit retry endpoint resumes it. Test: `tests/test_job_lifecycle_wiring.py::test_cancel_attempts_to_stop_the_rq_job`, `::test_cancel_survives_redis_outage`, `::test_worker_start_cannot_resurrect_a_cancelled_job` |
| F2 | P1 | User retry left the row pending forever: transition to `pending` without clearing `rq_job_id`; maintenance `re_dispatch_deferred_conversions` skips rows carrying an `rq_job_id` | `pdfword/worker_api.py` (`retry_user_document`), `pdfword/maintenance.py:25-33` | Retry clears `rq_job_id` then calls `_dispatch_conversion_job` (which re-enqueues, deleting any stale RQ job); if Redis is down the row stays pending with empty `rq_job_id` and maintenance recovers it. Test: `::test_retry_clears_stale_rq_job_id_and_redispatches`, `::test_retry_survives_redis_outage_and_maintenance_redispatches` |
| F3 | P2 | `DistributedJobQueue.enqueue` check-then-act TOCTOU: two concurrent dispatchers (upload handler + maintenance thread) could rpush the same job id twice → duplicate execution (rq 2.12 has no duplicate guard) | `pdfword/job_queue.py:102-129` | Dispatch serialized by a Redis `SET NX EX 30` lock per job; losers raise → dispatch is deferred, maintenance retries. Tests: `::test_enqueue_dispatch_lock_prevents_duplicate_queue_entries`, `::test_enqueue_dispatch_lock_rejects_concurrent_dispatch` |
| F4 | P2 | `transition_conversion` ran its claim SELECT in sqlite's autocommit (deferred transaction): two workers could both observe `pending` and both claim the job | `pdfword/database.py:1920-2005` | `transaction(immediate=True)` (BEGIN IMMEDIATE) added and used by `transition_conversion` and `consume_guest_result_token`; the status/single-use checks now share the write lock with the UPDATE. Test: `::test_single_use_guest_result_token_is_concurrent_safe` (deterministic: exactly one of two concurrent token consumes wins) |
| F5 | P2 | Backup zip written directly to its published name: crash leaves truncated archive; two same-second backups truncated each other | `pdfword/backup.py:28-55` | Zip staged under a unique `.part` name in the destination dir, fsynced, then `os.replace`; same-second collision gets a uuid suffix instead of overwriting. Test: `tests/test_atomic_publication.py::test_same_second_backups_do_not_overwrite_each_other` |
| F6 | P2 | Final DOCX written with direct `write_bytes` on worker/local paths (crash → truncated deliverable, row still marked completed) | `pdfword/conversion_service.py:323, 503` | Both publish via `atomic_write_bytes` (unique same-dir staging + fsync + replace). Exercised by existing conversion tests (`tests/test_conversion_service_paths.py`, `tests/test_distributed_worker.py`) |
| F7 | P2 | `save_checkpoint` used a fixed `checkpoint.json.tmp` name (concurrent writers interleave → corrupt checkpoint silently loads as empty; stale `.tmp` never cleaned) and no fsync | `pdfword/checkpoints.py:21-29` | Uses `atomic_write_text` (unique staging + fsync + replace). Test: `tests/test_atomic_publication.py::test_checkpoint_roundtrip_leaves_no_tmp_residue` |
| F8 | P3 | `LiveConversionProgress` same fixed-`.tmp`/no-fsync pattern for `progress.json` | `pdfword/conversion_service.py:135-137` | Uses `atomic_write_text` (same helper). Exercised by existing progress tests |
| F9 | P3 | Lifecycle audit report (evidence for a destructive op): second-resolution filename collisions overwrote the first run's sha256 inventory; non-atomic write | `clouda_data/lifecycle.py:103-110` | Collision gets a uuid suffix; written via `clouda_data.pretraining.hashing.atomic_write_text` |
| F10 | P3 | `quality clean-manifest` never ran the documented `revalidate_derived` fail-closed post-write re-check | `clouda_data/quality/cli.py:187-217`, `derived.py:7-9,233` | Called after `write_clean_manifest`; violations now raise. `tests/quality/` suite (249 tests) passes with the backstop active |
| F11 | P3 | `storage.safe_component` accepted Windows reserved device names (`CON.pdf` → uncreatable path), diverging from hardened `tenant_storage.safe_filename` | `pdfword/storage.py:13-16` | Reserved-device regex prefixes `_` (mirrors tenant_storage). Test: `tests/test_atomic_publication.py::test_safe_component_neutralizes_windows_reserved_devices` |
| F12 | P3 | Dead symlink guard: `path.resolve().is_symlink()` is always False (resolve follows links) | `pdfword/tenant_storage.py:31-35` | Checks `path.is_symlink()` before resolving; containment check unchanged |

Also fixed during review: the backup fsync initially placed outside the
`ZipFile` context (AttributeError on closed `fp`) — caught by the new
regression test before it shipped.

## Confirmed findings — NOT fixed (behavioral/design decisions; need owner sign-off)

| ID | Sev | Finding | Location | Why not fixed here |
|---|---|---|---|---|
| ~~U1~~ | P2 | **RESOLVED 2026-09-22 (v0.2.1 readiness session)**: text near-dup tier wired into the gate (LEAK_NEAR_TEXT critical + DUP_TEXT_NEAR warnings) with 8 regression tests — see `RELEASE_READINESS_V0.2.1.md`. Original finding:  Text near-duplicate tier (`classify_text_pairs`, MinHash/LSH) is dead code — the quality gate imports only its version constant, so near-duplicate **text** leakage across splits is never detected (exact-hash L5 only). Two texts differing only in diacritics/tatweel have different `normalized_text_sha256` and pass | `clouda_data/quality/gate.py:50-54,161-179`, `text_dup.py:184`, `leakage.py:451-481`, `pretraining/normalize.py:56-68` | Wiring it in changes gate verdicts on real corpora (new FAIL modes); needs a policy decision + golden-run comparison. Highest-value next fix. |
| ~~U2~~ | P2 | **RESOLVED 2026-09-22**: `write_clean_manifest` refuses FAIL verdicts (`FailedVerdictRefusedError`); CLI returns 1 with `clean_manifest: not_written`; forensic opt-in `allow_failed_verdict=True`. Original finding:  `quality clean-manifest` writes the "clean" manifest even on FAIL verdict; ERROR-severity artifact findings (`HASH_MISMATCH`, `IMAGE_DECODE`, `NON_EMPTY_FILE`) exclude nothing — a FAIL clean manifest can contain the mismatching row | `clouda_data/quality/cli.py:187-217`, `derived.py:72-196`, `policy.py:151-164` | Semantics of the artifact contract (write-with-verdict-header + exit 1 vs refuse) need owner decision; header does record `verdict: fail` |
| ~~U3~~ | P2 | **RESOLVED 2026-09-22**: digest trust model — unpinned sample downloads fail closed (`CLOUDA_ALLOW_UNPINNED_DATASET_DOWNLOADS=1` opt-in), `digest_source: registry_pinned|self_reported` recorded per file. Original finding:  Dataset download authenticity: sha256 enforcement is conditional on the registry asset carrying one; self-computed digests record integrity, not authenticity | `clouda_data/datasets/downloader.py:259, 334` | Requires pinning digests in `dataset_catalog/registry/*.json` — a content decision per source |
| U4 | P2 | CER/WER computed on raw strings in `evaluation/execution.py` vs normalized strings in `results/metrics.py` — same records score differently on the two canonical paths | `clouda_data/evaluation/execution.py:37-38`, `results/metrics.py:44-50` | Choosing the canonical view changes published-style numbers; needs benchmark owner sign-off |
| U5 | P2 | Three divergent Arabic normalizations (dataset split/leakage vs text-dup policy vs scoring fold_digits) → equivalence classes differ per subsystem | `pretraining/normalize.py:56-68`, `quality/text_dup.py:34-48`, `ground_truth/normalization.py:24-51` | Same as U4; document + unify deliberately |
| U6 | P2 | Cross-DB duplicate-dispatch residual: `abandon_stale_processing` resets a stale `processing` row to `pending` with `rq_job_id` still set; on a single-worker Windows deployment nothing re-enqueues until a worker calls `start` (RQ startup maintenance usually covers this, but it is not guaranteed) | `pdfword/database.py:2205-2222`, `maintenance.py:25-33` | Fix overlaps the F2 machinery (clear `rq_job_id` on abandon) but changes recovery semantics mid-flight; recommended follow-up |
| U7 | P2 | Benchmark pages were drawn from `source_split: "train"` of external HF datasets; no in-repo artifact proves benchmark-vs-training-corpora dedup was run for the published cohort | `benchmarks/ocr_arabic/benchmark_manifest.jsonl` (per-row `source_split`), `clouda_lab/holdout_guard.py` | Evidence lives in the private pipeline; add a recorded dedup artifact when the 462-page expansion resumes |
| U8 | P3 | Guest quota gate `guest_has_active_job` is check-then-act across separate calls; two concurrent `POST /guest/trial` can both pass (no unique constraint). Abuse-limited, not corruption | `pdfword/worker_api.py:1385-1386`, `database.py` | Needs a DB constraint; left as hardening |
| U9 | P3 | Fixed-`.tmp` writers remain in single-writer-at-a-time paths: `pretraining/workflow.py:269-280,398-410` (no fsync), `distortion/workflow.py:84-99` (fsync, no unique temp), `rendering/pipeline.py:163-172`, `training/exporter.py:225-229`, `runtime/torch_backend.py:210-212`; pdfword state stores `provider_router.py:39-44`, `ai_model_router.py:77-82`, `self_learning.py:49-54` (concurrent save → silent reset via `_safe_load`) | listed files | All currently single-writer by construction (grep-verified call sites); promote the shared hardened helper (e.g. move `pdfword/atomic.py` / `pretraining.manifest` pattern into `clouda_contracts`) as a mechanical follow-up |
| U10 | P3 | `lifecycle.archive_run` TOCTOU (exists-check then replace) + same-second trash-dir names can overwrite a recoverable copy; `run_id` "unknown-evidence" collisions in results ingest; `time.monotonic()` persisted into evaluation reports | `clouda_data/lifecycle.py:93-101,147-160`, `results/ingest.py:264-271`, `evaluation/execution.py:97` | Low likelihood; batch with U9 cleanup |
| U11 | P3 | Latent: `JobQueue._jobs` never pruned; `create_job_storage` (with F11 fixed) has no production callers; `EngineRegistry.register` unlocked per-call; budget lock held across SQLite I/O in local path | `pdfword/job_queue.py:27`, `pdfword/storage.py:42`, `pdfword/engines.py:103-112,343-366`, `conversion_service.py:436-444` | No live production path today |

## Verified safe (high-value negative checks)

- **Claim/finalization state machine** — token/status-predicated conditional UPDATEs; `.docx.part` + fsync + `os.replace` with DB claim protocol and rollback (`worker_api.py:2197-2258`, `database.py:2057-2237`). Duplicate result uploads: exactly one winner (tested).
- **Upload path** — `tenant_storage.write_upload`: O_CREAT|O_EXCL reservation, same-dir temp, fsync, replace; reserved-name sanitization; server-generated uuid output names; streaming size caps; magic/page-count checks (`worker_api.py:1195-1247,1379-1494`).
- **Archive handling** — no `extractall` anywhere; every zip validated by `clouda_contracts/archive_security` (zip-slip, bombs, symlinks, case-collisions) and extracted member-by-member with containment assertions (`backup.py`, `clouda_contracts/archive_security.py`).
- **Subprocess** — two sites only, both allowlisted argv, no shell (`local_ocr_adapters.py:288-298`, `doctor/system.py:228`).
- **Deserialization** — only `yaml.safe_load` (9 sites), defusedxml, no pickle/joblib; `torch.load(weights_only=False)` digest-guarded at both callers (`runtime/torch_backend.py:231,253`) and unreachable from network input.
- **Split/holdout integrity** — hash-bucket seeded splits with union-find grouping; report re-verifies disjointness; holdout excluded from export and training handoff fails closed on protected rows (`splitting.py`, `training_data/input_contract.py`, `clouda_contracts/protection.py`).
- **Results store** — domain-tagged sha256 identity, immutable run fields, per-run locks, full `verify_bundle` re-hashing (`clouda_data/results/store.py`).
- **Key-router reservations** — BEGIN IMMEDIATE + idempotency dedupe + fencing tokens + orphan reconciliation (`key_router/repository.py`).
- **Lab task/confirmation services** — RLock-protected atomic state writes, orphan recovery on restart (`clouda_lab/dashboard/tasks.py`, `confirmations.py`).

## Bandit triage (source packages)

- B613 `_raqm/corpus.py:59` — intentional Arabic bidi control chars (RLM/LRM corpus constants). False positive.
- B614 `checkpoint_torch.py:47` — digest-verified; hardening note only (see SECURITY_AUDIT_CURRENT.md).
- B105 `worker_api.py:61` — regex pattern, not a credential. False positive.
- B110-family (62 Low) — triaged samples are logged/degraded paths, not silent corruption; see SECURITY_AUDIT_CURRENT.md for the categorized list.
